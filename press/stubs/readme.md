# Project

This application was generated from a template. Its source identity is recorded
in [the press receipt](press/press-receipt.toml), under `[press.from]`.

Start with [project setup](docs/PROJECT_SETUP.md). Run `just setup` to install
the development environment, `just check` to validate it, and `uv build` to
build distributions. The application command is listed in `pyproject.toml`
under `[project.scripts]`.
